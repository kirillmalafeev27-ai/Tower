services:
  - type: web
    name: drucker-game
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: AITUNNEL_API_KEY
        sync: false
      - key: AITUNNEL_MODEL
        value: gpt-5.4
